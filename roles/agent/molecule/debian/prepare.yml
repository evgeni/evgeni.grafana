---
- name: Prepare
  hosts: all
  gather_facts: true
  tasks:
    - name: Install python3-debian
      ansible.builtin.apt:
        name: python3-debian
        update_cache: true
    - name: Setup repository
      ansible.builtin.include_role:
        name: repository
